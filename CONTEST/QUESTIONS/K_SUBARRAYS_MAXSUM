Given an integer array arr, you should partition the array into  k (contiguous) subarrays . After partitioning, each subarray has their values changed to become the maximum value of that subarray.
Find thr maximum sum of such an array.